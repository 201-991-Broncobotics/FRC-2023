package frc.robot.commands.autoDriveCommands;

public class DriveToNearestCone {
    // Y Values:
    // Short Left: 4.98
    // Short Right: 3.87
    // Center Left: 3.3
    // Center Right: 2.18
    // Long Left: 1.63
    // Long Right: 0.5

    // Intial X: 2.1
    // Final X: 1.85

    // Only do it if we're already in the zone. That is, (x, y) < (2.85, 5.5). Note this has to be adjusted for red
}
